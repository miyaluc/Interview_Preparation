using System;
using System.Collections.Generic;
using System.Text;

namespace InterviewPrep
{
    class ReturnMiddleElement
    {
        //return the middle element of a linked list

        /// <summary>
        /// Traverse linked list using two pointers. Move one pointer by one and other pointer by two. When the fast pointer reaches the end, the slow pointer will reach middle of linked list.
        /// </summary>
        public void PrintMiddle()
        {
            LinkedList<int> ll = new LinkedList<int>();
            
            Node head;

            Node walker = head;
            Node runner = head;

        }
    }
}
